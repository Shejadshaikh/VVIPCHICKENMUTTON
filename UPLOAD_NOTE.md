# VVIP MARKET PALACE V6 Upload Package

This is the V6 production foundation package.

Important:
- Upload this package to the `v6-production` branch, not `main`.
- This package is a development/production foundation, not the current live premium homepage.
- Do not replace the current `main` branch/live site with this package.
- The root `index.html` is the V6 customer module entry point.
- Payment secrets must never be uploaded to GitHub.
